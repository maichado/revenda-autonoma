# Dependências e build
node_modules/
dist/
dist-ssr/
.vite/

# Cache TypeScript
*.tsbuildinfo
tsconfig.tsbuildinfo
tsconfig.node.tsbuildinfo

# Variáveis de ambiente e segredos (NUNCA commitar)
.env
.env.local
.env.development.local
.env.production.local
.env.pb.local
.env.ngrok.local
scripts/secrets.ps1
ngrok.yml
ngrok-url.txt
pocketbase-url.txt

# PocketBase em runtime (binário + banco SQLite)
# Pasta de dados PocketBase (irmã do repositório — fora do Git)
../rvd-autonoma-pb/
rvd-autonoma-pb/
# Legado (antes da renomeação)
../gm-revenda-pb/
gm-revenda-pb/
pocketbase/pb_data/
pocketbase/pocketbase.exe
pocketbase/*.zip

# Backups locais do banco
pb_data_backup_*/
*.db-journal
*.sqlite-journal

# Logs e temporários
*.log
npm-debug.log*
.tools/
.DS_Store
Thumbs.db
Desktop.ini

# IDE (opcional — extensions.json pode ser compartilhado)
.idea/
.vscode/*
!.vscode/extensions.json
