/** Logo estática padrão (public/). */
export const LOGO_PADRAO_SRC = '/logo-revenda.png'

/** Tamanho máximo do arquivo antes do processamento (KB). */
export const LOGO_MAX_ARQUIVO_KB = 1024

/** Dimensões máximas após redimensionamento (px). */
export const LOGO_MAX_LARGURA = 512
export const LOGO_MAX_ALTURA = 512

/** Tamanho máximo do base64 salvo no PocketBase (KB). */
export const LOGO_MAX_BASE64_KB = 1024

/** SVG permitido com limite menor. */
export const LOGO_MAX_SVG_KB = 512
